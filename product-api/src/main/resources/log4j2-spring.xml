<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <!-- Standard pattern with MDC values -->
        <Property name="PATTERN_STANDARD">
            %d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] %30.30logger{1.} | %-30.30t >>> %msg%n
        </Property>
    </Properties>

    <Appenders>
        <!-- Console appender with a standard pattern -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${PATTERN_STANDARD}"/>
        </Console>

        <!-- Rolling file appender with a standard pattern -->
        <RollingFile name="RollingFile"
                     fileName="/julianrazif/microservices/logs/api/product-log.log"
                     filePattern="/julianrazif/microservices/logs/api/product-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${PATTERN_STANDARD}"/>
            <Policies>
                <OnStartupTriggeringPolicy />
                <SizeBasedTriggeringPolicy size="100 MB" />
                <TimeBasedTriggeringPolicy />
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Application-specific loggers -->
        <Logger name="com.julian.razif.microservices.api.product" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Logger>

        <!-- Spring Framework with WARN level to reduce noise -->
        <Logger name="org.springframework" level="WARN" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Logger>

        <Logger name="com.julian.razif.microservices.api.product.configuration" level="DEBUG" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Logger>

        <!-- Root logger -->
        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Root>
    </Loggers>
</Configuration>
